---
title: "Problema 1 - Checkpoint 1 - Nossa pergunta"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE,
  warning = FALSE,
  message = FALSE,
  screenshot.force = FALSE,
  fig.cap = '',
  fig.align = 'center',
  fig.width = 10)
```

```{r}
library(tidyr)
library(dplyr)
library(readr)
```

## Descrição dos dados

Serao estudadas as séries:
```{r}
series <- read_csv("series_from_imdb.csv") %>% filter(series_name %in% c("Mr Robot", "Sense8", "The Walking Dead", "Orphan Black", "Vikings"))
unique(series$series_name)
```

## Melhor avaliada
Tentaremos analizar o quão bem avaliada é uma série

```{r}

```

## Regularidade dos episódios